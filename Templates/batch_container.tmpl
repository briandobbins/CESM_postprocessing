#!/bin/bash

#PBS -N {{ processName }}
#PBS -q {{ queue }}
#PBS -l select={{ nodes }}:ncpus={{ ppn }}:mpiprocs={{ ppn }}
#PBS -l walltime={{ wallclock }}
#PBS -A {{ project }}

source /etc/profile.d/escomp.sh

